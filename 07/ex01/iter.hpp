#ifndef ITER_HPP
# define ITER_HPP

#include <cstddef>

// template <typename T, typename F> こういう書き方はできます
template<typename T, typename F>
void iter(T array, const size_t len, F func) {
	// T array,ではなく別の形で例示されていたので、後でチェックしてね。
	// TODO: イテレータで行う処理をここに書く
}

#endif